org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true

# Mirrors the root build: the compiler-plugin test framework and the native (CMake) build tasks
# are not yet configuration-cache compatible.
org.gradle.configuration-cache=false
